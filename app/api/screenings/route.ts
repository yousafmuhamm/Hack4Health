// app/api/screenings/route.ts
import { NextResponse } from "next/server";

// Temporary placeholder so Next.js treats this as a valid route/module.
// You can replace this later with real screenings logic.
export async function GET() {
  return NextResponse.json({
    message: "Screenings endpoint not implemented yet",
  });
}
